name: Fork PR Checks

on:
  pull_request:
    branches:
      - 'main'

jobs:
  checks:
    name: Run checks on GitHub-hosted runner for fork PRs
    if: github.event.pull_request.head.repo.full_name != github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Checkout trusted composite action
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: .trusted-actions
          sparse-checkout: |
            .github/actions/code-quality-checks
          sparse-checkout-cone-mode: false

      - name: Run code quality checks
        uses: ./.trusted-actions/.github/actions/code-quality-checks
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run tests
        run: |
          BASELINE=$(cat .github/coverage-baseline.txt)
          uv run pytest --cov=app --cov-report=term-missing --cov-fail-under="$BASELINE"
