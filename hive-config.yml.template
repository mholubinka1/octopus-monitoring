hive:
  username:
  password:

mariadb:
  host:
  port:
  database:
  username:
  password:

# Everything below is optional -- omit these sections entirely for a
# #506-only deployment (heating status polling doesn't use any of them).
# Open-Meteo needs no API key -- only Weather Underground and ntfy.sh do.

# Not read until weather observation polling ships (issue #508).
weather_underground:
  api_key:
  station_id:

# Not read until hive-app's re-auth alert path ships (issue #509).
ntfy:
  topic_url:

# Household lat/long, not read until weather queries ship (issue #508/#510).
location:
  latitude:
  longitude:
