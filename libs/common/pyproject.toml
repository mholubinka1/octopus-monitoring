[project]
name = "common"
version = "0.1.0"
description = ""
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0.52",
    "pymysql>=1.1.2",
    "pydantic>=2.13.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
